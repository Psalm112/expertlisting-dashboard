import { PlaceholderPage } from '@/components/layout/PlaceholderPage';

export const metadata = { title: 'Users | Expert Listing' };

export default function UsersPage() {
  return (
    <PlaceholderPage
      title="Users"
      description="Rider, subscriber, agent and developer records live on a dedicated screen not covered by the supplied design."
    />
  );
}
