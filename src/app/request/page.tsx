import { PlaceholderPage } from '@/components/layout/PlaceholderPage';

export const metadata = { title: 'Request | Expert Listing' };

export default function RequestPage() {
  return (
    <PlaceholderPage
      title="Request"
      description="Inbound requests from agents and developers are handled on a screen not covered by the supplied design."
    />
  );
}
